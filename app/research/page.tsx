// 과제용 2화면 MVP 시작점 — 조건 선택(S01). 결과는 /result(S02).
import { ConditionSelectScreen } from "@/components/screens/research-condition";

export default function Page() {
  return <ConditionSelectScreen />;
}
