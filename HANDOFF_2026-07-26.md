# 전공진화소 개발 인계서

- 인계일: 2026-07-26
- 저장소: `choihyungyu0/MajorEvolutionHomepage`
- 원격 저장소: `https://github.com/choihyungyu0/MajorEvolutionHomepage.git`
- 현재 브랜치: `feat/mentoring-mvp-professor-data`
- 기준 커밋: `61e71da6f0dcdde172187f24f33d4e1ba68ad5dc`
- 중요: 기준 커밋 이후의 수정·신규 파일은 아직 커밋하지 않은 작업물이다. 다른 PC에서 `git reset`, `git checkout --`, `git clean`으로 지우지 않는다.

## 1. 가장 먼저 읽을 내용

전공진화소의 해커톤 핵심 결과는 단순 교수 검색이 아니다.

> 학생이 연구 아이디어를 만들고, 공식 연구 근거로 맞는 교수를 찾고, 준비된 상태로 찾아가며, 받은 피드백을 실제 연구 수정과 다음 행동으로 바꾸는 서비스

교수 중심 핵심기능은 다음 3개다.

1. **교수 레이더**
   - 선택한 아이디어와 연결되는 교수 1명과 대안 1명
   - `주제·방법·응용 맥락` 역할별 연결 이유
   - 대학 공식 프로필과 공식 프로필에 노출된 논문 근거
2. **교수 Knock Kit**
   - 먼저 읽을 논문
   - 60초 자기소개
   - 논문 기반 질문 3개
   - 20분 면담 안건
   - 면담 요청 이메일과 연구예절
3. **Mentor Loop**
   - 교수 피드백 기록
   - 기존 기획안과 수정안 비교
   - 추천 자료·데이터·방법 정리
   - 학생의 다음 행동과 후속 연락

서비스 문구에서는 `교수님을 이용한다` 대신 `교수의 전문성과 학생의 준비도를 연결해 상호 가치 있는 협업을 만든다`를 사용한다.

## 2. 다른 PC에서 바로 시작하기

ZIP을 압축 해제하면 저장소의 `.git` 디렉터리와 미커밋 작업트리가 함께 들어 있다. 별도 clone 없이 이어갈 수 있다.

### Windows PowerShell

```powershell
cd .\MajorEvolutionHomepage

# Git 상태와 브랜치를 먼저 확인한다.
git status --short --branch
git branch --show-current

# Node.js 20 이상이 필요하다.
node --version

# node_modules와 .next는 패키지에서 제외했다.
npm.cmd ci

# 환경변수 파일을 만든다. 값은 직접 입력하고 커밋하지 않는다.
Copy-Item .env.example .env.local

# 검증
npm.cmd run typecheck
npm.cmd run professors:test
npm.cmd run professors:smoke
npm.cmd run build

# 로컬 실행
npm.cmd run dev
```

PowerShell 실행 정책이 `npm.ps1`을 차단할 수 있으므로 Windows에서는 `npm` 대신 `npm.cmd`를 사용한다.

### 필요한 환경변수

`.env.example`에 이름만 들어 있다.

```dotenv
NEXT_PUBLIC_SITE_URL=http://localhost:3000
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5-mini
```

- `OPENAI_API_KEY`는 서버 전용이다.
- 실제 키를 ZIP, Git, 문서, 스크린샷에 넣지 않는다.
- `NEXT_PUBLIC_` 접두사가 붙은 변수에는 비밀값을 넣지 않는다.

## 3. 현재 Git 상태

현재 브랜치는 원격의 같은 이름 브랜치를 추적한다.

```text
feat/mentoring-mvp-professor-data
origin/feat/mentoring-mvp-professor-data
```

기준 커밋 이후 주요 변경량:

```text
11 tracked files changed
950 insertions
32 deletions
다수의 신규 공동설계·교수 수집 파일 존재
```

주요 수정 파일:

- `README.md`
- `app/globals.css`
- `components/screens/entry-screens.tsx`
- `components/screens/research-condition.tsx`
- `components/screens/research-result.tsx`
- `data/research-mvp.ts`
- `lib/ai-client.ts`
- `lib/openai-server.ts`
- `lib/recommend.ts`
- `package.json`
- `store/research-store.ts`

주요 신규 파일·폴더:

- `app/co-design/`
- `app/api/ai/co-design/`
- `components/screens/co-design-screen.tsx`
- `data/co-design.ts`
- `lib/co-design-ai.ts`
- `data/professors/`
- `scripts/professor-crawler/`
- `docs/MVP_SPEC.md`
- `docs/PROFESSOR_DATA.md`
- `design/concepts/`

반드시 `git status --short --branch`로 실제 목록을 다시 확인한다.

## 4. 현재까지 완료된 제품 흐름

현재 새 MVP 흐름:

```text
/research
  → 3개 아이디어 탐색 모드와 연구 조건
/co-design
  → AI가 한 번에 한 질문
  → 확인된 사용자 맥락 누적
/result
  → 안전 축소형과 차별 심화형 후보 2개
  → 점수 없이 동일 스키마 비교
  → 한 주제 선택
  → 교수 공식 정보 연결 영역
```

확정된 제품 원칙:

- 3개 탐색 모드
  1. 학과 내부 자유 브레인스토밍
  2. 학과와 AI 연구 트렌드 결합
  3. 다른 전공과의 융합
- AI가 한 번에 한 질문을 한다.
- 사용자 확인 사실과 AI 제안을 분리한다.
- 후보는 정확히 2개를 동일 스키마로 비교한다.
- 실현성·교수 적합도를 0~100 점수로 표시하지 않는다.
- 교수 정보는 대학 공식 페이지를 기준으로 한다.
- 교수 논문은 공식 프로필에 노출된 목록만 교수 실적으로 취급한다.
- DOI/KCI/Crossref는 공식 목록에 이미 있는 논문의 식별자 보강에만 사용한다.
- 공식 프로필에 없는 정보는 추정하지 않고 상태로 표시한다.

## 5. 교수 중심 기능의 현재 준비도

화면·상호작용 골격은 약 60% 수준이지만, 실제 공식 교수 데이터 기반 E2E는 약 20~25% 수준이다. 3대 기능 중 실제 데이터로 끝까지 완주되는 기능은 아직 없다.

### 교수 레이더

이미 있는 것:

- `/professors` 역할 필터
- 교수 저장과 2명 비교
- 교수 상세, 추천 이유, 출처
- `/result`의 교수 연결 영역

현재 병목:

- `/professors`는 `data/prototype.ts`의 가상 교수 데이터를 사용한다.
- `lib/co-design-ai.ts`는 AI 후보의 `professorIds`를 항상 빈 배열로 만든다.
- `lib/openai-server.ts`는 공동설계 프롬프트에 `officialEvidence: []`를 전달한다.
- 따라서 새 흐름의 `/result`는 교수 연결 대기 상태로 끝난다.

### 교수 Knock Kit

이미 있는 것:

- 교수에게 물어볼 질문 생성
- 아이디어 3문장 요약
- `/quest`의 질문 3개 편집
- 면담 이메일 초안, 복사, 이메일 앱 열기
- 데이터·방법 계획과 4주 실행계획
- Markdown·ICS·인쇄 출력

현재 병목:

- 가상 교수·가상 출처를 사용한다.
- 새 `/research → /co-design → /result` 흐름과 연결되지 않았다.
- 실제 교수 논문을 읽은 흔적과 공식 근거가 이메일·질문에 주입되지 않는다.

### Mentor Loop

이미 있는 것:

- 프로젝트 실행 퀘스트
- 완료 상태와 로컬 저장
- 데이터·방법·일정 계획

없는 것:

- 교수 피드백 입력 모델
- 추천 논문·데이터·연구실 자원 기록
- 기획안 수정 전후 비교
- 다음 만남 전 학생의 약속
- 감사·후속 이메일
- `준비 → 연락 → 면담 → 후속` 관계 단계

## 6. 교수 데이터 수집 현황

데이터 스키마:

- `data/professors/schema.json`
- 범위: `OFFICIAL_PROFILE_LIST_ONLY`
- 상태:
  - `FOUND`
  - `NOT_LISTED_ON_OFFICIAL_PROFILE`
  - `PROFILE_UNAVAILABLE`
  - `PARSE_FAILED`
  - `ROBOTS_BLOCKED`

개인정보 원칙:

- 이메일 제외
- 전화·팩스 제외
- 사진·이미지 제외
- 허용된 공식 프로필·연구분야·공식 논문 목록만 저장

### 단국대학교

- 학과 단위 115개 발견
- 현재 상세 샘플: 국어국문학과 1개
- 저장 교수: 2명
- 공식 프로필 노출 논문: 111건
- 보고서: `data/professors/sample/live-dku/report.md`
- 현재 범위 상태: `SAMPLE`

### 충북대학교

- 현재 시드: 소프트웨어학부 1개
- 저장 교수: 2명
- 공식 프로필 노출 논문: 0건
- 논문 상태: `NOT_LISTED_ON_OFFICIAL_PROFILE`
- 중앙 학과 디렉터리는 robots 정책으로 전체 자동 발견 불가
- 보고서: `data/professors/sample/live-cbnu/report.md`
- 현재 범위 상태: `SAMPLE`

### 메타데이터 보강

- Crossref: 비활성화
- KCI: API 키가 없어 비활성화
- 단국대 샘플 DOI/KCI 식별자 매칭: 0건
- 제목만으로 DOI를 연결하지 않는다.
- 향후 제목·교수명/저자·연도를 함께 확인하고 불일치는 `AMBIGUOUS`로 차단한다.

## 7. 다음 작업 순서

### P0-1. 데이터 런타임 연결

목표:

- 수집 JSON을 앱에서 읽는 서버측 인덱스 생성
- `topicId`, `professorId`, `publicationId`, `evidenceId`를 안정적으로 연결
- `/research → /result → /professors → /quest`가 같은 식별자를 사용

완료 조건:

- 실명 교수 1명과 대안 1명이 공식 근거와 함께 `/result`에 표시된다.
- 가상대학교 교수는 새 흐름에 나타나지 않는다.
- 데이터가 없으면 이유 상태가 보인다.

### P0-2. 역할 기반 교수 매칭

점수 대신 다음 역할을 사용한다.

- `TOPIC`: 연구주제 연결
- `METHOD`: 방법론 연결
- `CONTEXT`: 응용 맥락 연결

각 추천에는 다음을 포함한다.

- 연결 역할
- 자연어 이유
- 공식 근거 ID
- 근거가 말하지 않는 부분
- 모집·면담 가능 여부는 항상 `UNKNOWN`
- 학교 공식 문의 페이지

### P0-3. 기존 교수·퀘스트 화면 재사용

- `data/prototype.ts` 의존을 실제 교수 도메인 모델로 교체한다.
- 레거시 교수 적합도 점수를 제거한다.
- `/result`에서 교수 선택 후 `/professors/[id]`와 `/quest`로 이어지게 한다.
- 새 `research-store`와 기존 `prototype-store`의 상태 단절을 해소한다.
- 새로고침 후 선택 주제·교수·준비 상태가 사라지지 않게 한다.

### P1. 교수 Knock Kit 실데이터화

출력:

- 왜 이 교수인지
- 먼저 읽을 공식 논문 1편
- 논문에서 이해한 내용 한 문장
- 내 연구와 연결되는 이유
- 60초 자기소개
- 검색으로 해결할 수 없는 질문 3개
- 20분 면담 안건
- 면담 요청 이메일
- 연구예절 체크리스트

자동 이메일 발송은 하지 않는다. 사용자가 검토한 뒤 복사하거나 공식 문의 페이지를 연다.

### P2. Mentor Loop

추가할 입력:

1. 교수님이 수정하라고 한 연구범위
2. 추천한 데이터·방법·논문
3. 우려하거나 확인하라고 한 점
4. 학생이 하기로 약속한 일
5. 다음 확인 시점

추가할 출력:

- 기존 기획안과 수정안 비교
- 연구질문·데이터·방법 변경점
- 학생의 7일 행동계획
- 교수에게 요청할 다음 피드백
- 감사·후속 이메일

## 8. 해커톤 데모 범위

전 학과 데이터 수집 목표는 유지한다. 다만 해커톤 E2E 데모는 양 대학에서 검증 완료된 대표 학과를 한 곳씩 선택해 깊게 보여준다.

3분 데모:

```text
전공·관심사 입력
→ AI 질문
→ 후보 2개 비교
→ 한 주제 선택
→ 공식 근거로 교수 선택
→ 논문·질문·면담 이메일 생성
→ 교수 피드백 입력
→ 수정 기획안과 다음 행동 출력
```

심사 설명:

> ChatGPT가 근거 없이 교수를 추천하는 것이 아니라, 대학 공식 프로필과 실제 논문을 근거로 학생이 준비된 질문과 기여안을 가지고 찾아가도록 돕는다.

## 9. 현재 검증 결과

2026-07-26 기준:

```text
npm.cmd ci                   PASS
npm.cmd run typecheck        PASS
npm.cmd run professors:test  PASS (7/7)
npm.cmd run professors:smoke PASS
npm.cmd run build            PASS
```

교수 smoke 결과:

```json
{
  "smoke": "PASS",
  "valid": true,
  "record_count": 2,
  "university_count": 2
}
```

주의:

- smoke의 `record_count: 2`는 전체 교수 수가 아니라 smoke fixture 기준이다.
- 현재 스키마 검증기는 전체 JSON Schema 규칙을 완전하게 실행하는 구현이 아니다.
- 실제 배포 전 대표 학과 E2E 데이터 검증이 필요하다.

### 의존성 보안 감사

새 압축 해제본에서 `npm.cmd ci` 후 `npm.cmd audit`를 실행했으며, 2026-07-26 기준 high 3건이 보고됐다.

- 직접 의존성 `next@15.5.19`
  - App Router/Server Actions DoS
  - 일부 Server Actions·rewrite 구성의 SSRF
  - 권고 수정 버전: `15.5.21` 이상
- 전이 의존성 `sharp@0.34.5`
  - 포함된 libvips 계열 취약점
- 전이 의존성 `brace-expansion@1.1.15`, `brace-expansion@5.0.6`
  - 비정상 패턴 확장에 의한 DoS/OOM

자동 `npm audit fix`는 잠금파일과 프레임워크 버전을 바꿀 수 있으므로 인계 패키지 생성 중에는 실행하지 않았다.

다음 PC에서 우선 처리:

1. 별도 커밋 또는 별도 작업 브랜치에서 Next.js와 관련 잠금파일을 안전 버전으로 올린다.
2. `npm.cmd audit`로 `sharp`, `brace-expansion` 해소 여부를 확인한다.
3. 아래 검증을 모두 다시 실행한다.
   - `npm.cmd run typecheck`
   - `npm.cmd run professors:test`
   - `npm.cmd run professors:smoke`
   - `npm.cmd run build`
4. 검증이 끝날 때까지 외부 공개 배포를 보안 완료로 간주하지 않는다.

## 10. 알려진 맹점

- 교수 친밀도·성격·응답 가능성을 추정하지 않는다.
- 교수 합격·지도·면담 가능성을 보장하지 않는다.
- 자동 대량 이메일을 보내지 않는다.
- 공식 프로필에 없는 논문을 교수 실적으로 추가하지 않는다.
- 제목만 확보한 논문을 연구결과까지 읽은 것처럼 요약하지 않는다.
- 면담 메모는 미공개 연구 아이디어일 수 있으므로 최소 수집·삭제·내보내기가 필요하다.
- 충북대 전체 수집 차단 상태를 숨기지 않는다.
- PaperPal처럼 제목·DOI·PDF가 서로 다른 논문으로 연결되지 않도록 정체성 검증을 먼저 한다.
- 현재 의존성 감사 high 3건을 해소하기 전까지 보안 검토 완료로 표시하지 않는다.

## 11. 아직 확정하지 않은 결정

현재 권장 기본값:

- 해커톤 결과 범위: 면담 준비에서 끝내지 않고 피드백 반영 1회까지
- 실명 교수 사용: 검증된 공식 공개 프로필만 사용
- 연락처: 이메일·전화 저장 없음
- 피드백 입력: 녹음 없이 사용자가 구조화된 다섯 항목을 직접 입력
- 저장: 우선 로컬 저장과 내보내기
- 일반 논문 검색·서재·일일 알림: 해커톤 이후

## 12. 다음 Codex 작업에 그대로 전달할 프롬프트

```text
먼저 HANDOFF_2026-07-26.md를 끝까지 읽고 현재 git status를 확인해줘.
현재 미커밋 변경은 보존하고 reset/checkout/clean 하지 마.

해커톤 핵심은 교수 레이더, 교수 Knock Kit, Mentor Loop 3개야.
새 화면을 추가하기 전에 현재 /result, /professors, /quest를 재사용해.

첫 작업은 selectedTopicId를 실제 공식 교수 데이터와 연결하는 것이야.
data/professors 샘플을 서버측에서 읽고, 점수 없이 TOPIC/METHOD/CONTEXT 역할과 evidenceIds로 교수 1명+대안 1명을 연결해줘.
공식 프로필 미기재, 차단, 파싱 실패 상태를 숨기지 마.

구현 전 현재 구조를 다시 감사하고, 보안·예외·상태 영속성 맹점을 먼저 보고해.
구현 후 npm.cmd run typecheck, professors:test, professors:smoke, build를 실행하고 결과를 기록해.
```

## 13. 관련 문서

저장소 내부:

- `README.md`
- `docs/MVP_SPEC.md`
- `docs/PROFESSOR_DATA.md`
- `data/professors/schema.json`
- `data/professors/sample/live-dku/report.md`
- `data/professors/sample/live-cbnu/report.md`
- `design/concepts/`

인계 ZIP의 `context/` 폴더:

- 기존 전체 사용자 흐름
- 화면 명세
- 디자인 가이드
- 최종 기획안 DOCX
- 멘토링 사용자 흐름 이미지
- 멘토링 답변 확인 자료
- 2026-07-26 멘토링 자동 전사
- PaperPal 참고 스크린샷

패키지에서 의도적으로 제외한 항목:

- `node_modules`, `.next`, 빌드 캐시
- `.env`, `.env.local` 등 실제 환경변수 파일
- 원본 MP4 녹화 영상
- 기존에 생성된 중복 ZIP 파일

원본 영상은 용량 때문에 포함하지 않았으며, 인계에 필요한 발화 내용은 자동 전사본으로 포함한다.
