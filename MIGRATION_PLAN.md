# 전공진화소 개편 계획 (현재 앱 → 멘토링 반영 수정기획)

> 기준 문서: `전공진화소_멘토링반영_수정기획안_20260714.docx`, `USER_FLOW.md`, `PROMPTS.md`
> 대상 저장소: `MajorEvolutionHomepage` (Next.js 15 / React 19 / Zustand / OpenAI)
> 작성 목적: **코드 변경 전** 현재 앱과 새 기획의 차이를 정리하고, 무엇을 유지·개편·제거·이동할지와 단계 계획을 확정한다.

---

## 0. 요약

- **현재 앱 = 멘토링에서 "좁히라"고 지목한 초기 통합안 쪽이다.** 수정기획안 §9.3은 사실상 *"현재 12개 UI 시안을 이렇게 바꿔라"*는 지시이고, 대표 시나리오(§18)도 지금 앱과 같은 페르소나(수학+식품경제+ESG)를 쓰지만 **결과물 구조가 완전히 다르다**.
- 이번 개편은 버튼·문구 수준 조정이 아니라 **정보구조 + 데이터 계약 + AI 파이프라인의 재설계**다. 다만 시각 시스템(색·카드·칩·시트·모바일 레이아웃)과 일부 화면 골격은 재사용 가능하다.
- **가장 큰 신규 요소는 S03 "가이드형 AI 공동설계 대화"** 다. 현재 앱은 단발 생성(AI가 아이디어 3개를 한 번에 던짐)이라 이 대화 흐름 자체가 없다.
- **직전에 다듬은 것 중 상당수가 제거 대상**이다: 0~100 점수 막대(#2), 난이도 맛 라벨(#6), 실행 퀘스트/4주(#12 관련), 교수 적합도 점수 등. → 아래 §3 참고.

### 두 개의 목표 스코프

| 스코프 | 정의 | 화면 | 추천 방식 | 성격 |
|---|---|---|---|---|
| **A. 과제용(USER_FLOW.md)** | 2회차 과제 1차 MVP | **2화면** (S01 조건선택 → S02 추천결과 2개 비교) | **검수 로컬 데이터 필터링** (AI 생성/대화 없음) | 가장 빠른 종단 데모 |
| **B. 기획안(docx)** | 10주 파일럿 P0 | **7화면** (S01~S07) + 브리프 재개 + 관리자 최소 | **AI 가이드 대화 + 로컬 근거** | 완성형 MVP |

> **권장 경로**: A를 먼저 완주(종단으로 도는 것 검증) → B의 대화/브리프/교수연결로 확장. 위험 항목 §17(범위 과다로 종단 미완성)과 §14 로드맵의 "3주차에 seed로 S01~S07 먼저 이동" 원칙과도 부합한다.

---

## 1. 화면 매핑 (현재 → 목표)

현재 라우트(제가 전수 감사로 확인한 실제 구조) 기준.

| 현재 화면·라우트 | 처리 | 목표(기획안) | 핵심 변경 |
|---|---|---|---|
| `/` 스플래시 | **개편** | S01 시작·적합 확인 | 단일 페르소나 카피 + "AI와 질문 좁혀 브리프 완성" 약속 + **서비스가 못 하는 것(보장 없음) 명시** |
| `/goal` 4개 목적 선택 | **제거/축소** | S01에 흡수 | 4개 목적(전공탐색/주제/교수/논문) 동시 제시 X → 단일 시작 목적 + 적합 확인. 논문·프로젝트·서비스 시작은 P1↑ |
| `/dna` 6단계 입력 | **개편** | S02 목표·맥락 입력 | 상단개요/하단상세 중복 통합(§9.3), 짧은 초기폼 + **점진적 질문**. 맥락 심화(수강과목·과제 역할·도구 수준·데이터 접근) |
| `/analyzing` 분석 로딩 | **유지(축소)** | S02→S03 전환 로딩 | 단발 생성 로딩 → 대화 진입/근거 검색 로딩으로 의미 변경 |
| `/evolution-report` 리포트 | **개편** | S03 연구 방향(전반) | 레이더 **수치 강조 제거**, "연결도 84%" 류 제거. 방향 2~3개 + **출처·검증일** + 연결 이유. "연결 높음" 배지는 근거 문장으로 |
| — (없음) | **신규(최우선)** | **S03 가이드형 공동설계 대화** | 현재 앱에 없음. 한 번에 한 질문으로 문제·대상·데이터·방법·범위를 좁힘. **AI 제안 / 확인 사실 / 미확인 가정 분리** |
| `/ideas` AI 아이디어 3개 | **개편** | S04 후보 2개 | 연구/프로젝트/서비스 3카드 → **연구형 후보 2개만**(안전 축소형 A / 차별 심화형 B). 점수 막대·북마크 흐름 제거 |
| `/ideas/compare` 비교 | **개편** | S04 비교·선택 | **0~100 점수·막대 전면 제거** → `확인됨/조건부/확인 필요` + 근거 문장. 난이도 4단계 그리드 → **4주 축소형 / 학기 확장형** 2안 |
| `/feasibility` 실현성 | **부분 유지·통합** | S04/S05 조건 확인 | 이미 정성(내 #7이 green/amber 2단계로 정리 → 기획 방향과 부합). 점수화 함의만 제거하고 항목 재정의(데이터/방법/기간·범위/비용·도구/윤리·권리/평가 방식) |
| `/passport` 패스포트 | **개편(핵심)** | S05 연구주제 브리프 | 새 스키마로 재구성: 한 줄·문제·연구질문·**개인연결(확인 사실 vs AI 추론 분리)**·선행근거+출처·데이터후보(status)·방법후보·범위·조건/불확실성·교수연결·다음행동 |
| `/professors` 교수 추천 | **개편** | S06 교수 연결 | **적합도 점수링(89 등)·근거 신뢰도 점수 제거**. 브리프 확정 뒤 진입(순서는 이미 맞음). 공식 근거·검증일·확인 질문 |
| `/professors/[id]` 교수 상세 | **개편** | S06 상세 | 점수 제거, **공식 문의 URL(새 탭 이동) + `availability_unknown` 고지** 강화. (이미 "직접 확인할 점"·가상데이터 고지 있음 → 자산으로 재사용) |
| `/quest` 실행 퀘스트 | **대폭 축소** | S07 면담 준비·공식 이동 | 6단계 퀘스트·4주 자동계획·성장 아카이브 → **P1로 이동**. P0는 질문 3~5개·이메일 초안·**첫 30분 행동**·공식 문의 링크만 |
| `/home` 홈 | **축소/보류** | (MVP는 단일 가이드 흐름) | 하단 5탭 전부 구현 X(§9.1). 홈 대신 브리프 재개(P01) 최소 |
| `/explore` 탐색 | **보류** | P1 | MVP 미구현 |
| `/saved` 보관함 | **축소** | P01 브리프 재개 | 저장 아이디어·교수 목록 → **브리프 저장·수정·재개**로 축소 |
| `/profile` 마이 | **축소** | 마이(최소) | 연구 맥락·대화 재개·브리프 수정·삭제·동의 관리 중심 |
| `/paper` 논문 이해 | **P1 이동** | (P1) 논문 해부실 | MVP는 검수 연구방향의 출처 링크 수준만. 정식 논문 분석은 P1 |

---

## 2. 제거 대상 (직전 감사에서 다듬은 항목 포함 ⚠️)

새 기획이 **명시적으로 없애라고 한 것**(§10.3, §12.1, §13.5). 직전 4커밋에서 손본 것 중 아래는 **개편 시 삭제/대체**된다.

| 항목 | 현재 위치 | 처리 | 근거 |
|---|---|---|---|
| **0~100 점수·막대** (직전 #2 수정) | `data-visuals.tsx` CriteriaBar, `idea-screens.tsx` compare, radar 수치 | **제거** → 확인됨/조건부/확인 필요 | §8.4, §12.1, 데이터 계약에서 수치 적합도 필드 삭제(§10.3) |
| **난이도 맛 라벨**(순한맛~아주 매운맛, 직전 #6) | `data/prototype.ts` difficultyCopy.spice, 난이도 그리드 | **제거** → 4주 축소형/학기 확장형 | §2.3, §7.4, §13.5 (4단계 난이도 모듈 제외) |
| **연구/프로젝트/서비스 3아이디어** | `/ideas`, prototype.ts ideaSets | **후보 2개(연구형)로** | §7.4, §13.5 |
| **교수 적합도 점수(89)·근거 신뢰도 점수** | `/professors` score-ring, professor.matchScore | **제거** → 근거·검증일·확인 질문 | §8.5(수치 점수 미사용), §12.1 |
| **실행 퀘스트 6단계·4주 자동계획** | `/quest` | **P1 이동**(P0는 질문·메일·첫30분만) | §8.7, §13.3 |
| **성장 아카이브·포트폴리오** | (일부 홈/퀘스트) | **P1 이동** | §8.8, §13.3 |
| **4개 목적 동시 선택** | `/goal` | **단일 적합 확인으로** | §4.2 |
| **독립 AI 챗봇 상정** | (계획상) | **삭제 → S03 가이드형 공동설계** | §9.3 |

---

## 3. 데이터·상태 모델 변경

### 제거할 필드
- `Idea.scores` (personalFit/majorFit/dataAccess/feasibility/careerValue/novelty, 0~100)
- `professor.matchScore`, 근거 신뢰도의 수치화
- `difficultyCopy[*].spice`, `difficulty` 4단계
- `dna.radar`의 수치 강조(시각화는 유지 가능하나 백분율/점수로 제시 X)

### 신규 스키마 (기획 §10.4 데이터 계약)
```jsonc
research_brief: {
  title, problem, question,
  user_confirmed: [...],          // 사용자가 직접 확인한 사실
  ai_inferred: [...],             // AI 추론 (분리 표기)
  evidence_ids: [source_uuid...], // 근거 출처 ID
  data_options: [{ name, status:"확인됨|조건부|확인 필요" }],
  method_options: [...],
  scope_limits: [...],
  open_questions: [...],
  next_action                     // 첫 30분 행동
}
professor_link: {
  professor_id,
  match_reasons: [{ reason, source_id }],
  verified_at, official_contact_url,
  availability_unknown: true
}
```

### 로컬 검수 데이터 계층 (신규)
- **연구 방향** 10~15개, **교수** 15~30명, **출처** 50개+, 공식 문의 URL 15개+ — 각 항목에 `source_id`, `verified_at` (§11.5).
- seed 예시는 **아이디어 뱅크가 아니라** 대화 구조·품질 평가용 (§7.2, §11.5).

---

## 4. AI / 백엔드 변경

| | 현재 (`lib/openai-server.ts`) | 목표 (기획 §10) |
|---|---|---|
| 방식 | **단발 생성**: journey(dna+trends+ideas 3개+radar, 0~100) 한 번에 | **7모듈 파이프라인** 1개 서버 워크플로우 |
| 모듈 | (없음) | Context Builder / Evidence Retriever / Dialogue Planner / Topic Co-designer / Condition Checker / Mentor·Contact Linker / **Grounding Validator** |
| 상호작용 | 1회 호출 → 결과 | **다회차 가이드 대화**(한 번에 한 질문) |
| 출력 | 점수 포함 JSON | **점수 없는** 구조화 출력 + `source_id`/`verified_at`/`availability_unknown`, 과장·보장 표현 검증 |
| 근거 | 모델 자유 생성 | **허용 출처·ID 안에서만**, 미확인은 가정으로 표기 |

> **과제용 A**는 이 파이프라인 없이 **로컬 필터**로 단순화 가능(§8 USER_FLOW: 전공 필터→관심 연결→기간/데이터/방법 조건→충돌 제외→비교가치 있는 2개). AI 대화는 B에서 도입.

---

## 5. 재사용 가능한 자산 (버리지 말 것)

- **시각 시스템 전체**: `globals.css` 토큰(네이비/민트/블루/바이올렛), 카드·칩·버튼·시트·상태배지, 모바일 1열 레이아웃, 44px 터치.
- **feasibility의 확인됨/조건부/확인 필요** — 직전 #7로 green/amber 2단계로 정리해둔 것이 **새 기획의 조건 표기와 그대로 부합**한다.
- **professor 상세의 출처·검증일·"직접 확인할 점"·가상데이터 고지** — 신뢰 지향 구조가 이미 기획 §8.5 방향과 일치. 점수만 떼면 재사용.
- **passport 문서형 골격** — 브리프 스키마로 필드 재구성하면 재사용.
- 스플래시 크롭(직전 #1)·헤더/안내 마스코트, `AppShell`·`store-hydrator`, 라우팅 골격.

---

## 6. 카피·신뢰 원칙 반영 (§12, §20)

- **삭제**: "실현성 84/100", "적합도 87", "성공 가능성 높음", 순한맛/교수님맛/학회맛, "이 교수님은 반드시 답변".
- **추가**: 사실/추론/미확인 3분리(색+텍스트+아이콘), 모든 결과 "초안" 표시, `availability_unknown` 고지, "실제 가능 여부는 학교 공식 채널에서 확인", 외부 이동 안내.
- 온보딩·대화는 친근하게, **연구주제·교수연결·외부이동은 근거 중심·책임 경계 명확**.

---

## 7. 마이그레이션 단계 (로드맵 §14 반영)

| 단계 | 내용 | 산출 |
|---|---|---|
| **0. 계약 확정** | 데이터 계약에서 점수 필드 제거, `research_brief`/`professor_link` 스키마 확정, 로컬 검수 데이터(방향/교수/출처) seed 구축 | 데이터 계층·타입 |
| **1. 종단 골격(정적)** | S01~S07(또는 과제용 S01~S02)을 seed로 **처음부터 끝까지 이동**. 기존 화면 재배치, 점수/맛/퀘스트 제거 | 도는 종단 흐름 |
| **2. 맥락+대화** | S02 짧은폼+점진 질문, **S03 가이드 대화**(신규), 확인 상태 저장 | 개인 맥락·대화 |
| **3. 후보·브리프** | 후보 2개 서술형 비교, **S05 브리프 확정·수정·재개** | 브리프 객체 |
| **4. 교수·문의** | S06 공식근거+검증일, S07 질문·메일·첫30분·**공식 링크(새 탭)**+안전장치 | 문의 준비 |
| **5. 검증** | 고정 프로필 30개·후보 60개 품질 평가, 5~8명 종단 테스트, 허용외 출처 0건 | 파일럿 준비 |

> **과제용 A**는 0~1단계를 필터 버전으로 축약하면 가장 빠른 데모가 나온다.

---

## 8. 결정이 필요한 항목 (구현 착수 전 확인)

1. **스코프**: 과제용 **2화면(A)** 먼저 vs 기획안 **7화면(B)** 바로 착수?
2. **저장소**: 지금 `MajorEvolutionHomepage`를 개편 vs **새 저장소**에서 시작? (현재 앱은 재사용 자산은 많지만 라우팅/데이터/AI는 상당 부분 재작성)
3. **추천 방식**: 로컬 **필터**(USER_FLOW)로 갈지, **AI 가이드 대화**(기획안)까지 P0에 넣을지 — 백엔드 규모 차이가 큼.
4. **화면 세부 스펙**: `USER_FLOW.md`가 참조하는 **`SCREEN_SPEC.md`** 가 있으면 화면 요소는 그걸 우선. 없으면 이 계획을 토대로 화면 명세 초안을 먼저 작성.

---

*본 문서는 코드 변경 없이 작성된 개편 계획 초안입니다. 스코프/저장소/추천 방식이 정해지면 화면 명세와 데이터 계약부터 순차 착수합니다.*
