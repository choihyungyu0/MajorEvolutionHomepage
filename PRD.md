# 전공진화소 인터랙티브 웹 프로토타입 PRD

> 문서 버전: 1.2
> 작성일: 2026-07-13
> 구현 저장소: `C:\Users\최현규\Desktop\MajorEvolutionHomepage`
> 원격 저장소: `https://github.com/choihyungyu0/MajorEvolutionHomepage`
> 기준 화면: 모바일 390 x 844px
> 상태: OpenAI 연동 구현

## 0. 1.1 변경 사항

- 사용자의 최신 지시에 따라 실제 OpenAI Responses API 호출을 제품 범위에 포함한다.
- 전공 DNA, 탐색 방향 5개, 아이디어 3개와 아이디어 재생성을 strict JSON Schema로 생성한다.
- API 키는 서버 전용 환경 변수로만 사용하고, 실패 시 명시적인 샘플 fallback을 제공한다.
- 교수 후보와 출처는 여전히 기능 검증용 가상 데이터이며 실제 교수 검색으로 오해하지 않도록 표시한다.

### 1.2 기능 완성

- 논문 초록·본문의 질문, 방법, 결과, 한계, 용어, 후속 질문 분석을 실제 OpenAI 호출로 제공한다.
- 실행 퀘스트를 6개 완료 단계와 영속 진행률로 확장한다.
- 이메일 앱 열기, 캘린더 ICS, Markdown 프로젝트 문서, 인쇄 기반 PDF 저장을 제공한다.
- 고정 문장이던 리포트 조정과 교수 면담 도움을 실제 맥락형 AI 호출로 교체한다.
- 오프라인 상태를 앱 전체에서 감지해 AI 기능의 사용 가능 여부를 안내한다.

## 1. 결론

ZIP에 제공된 화면과 `design(1).md`에 정의된 기능은 모두 웹 프로토타입으로 구현 가능하다.

구현 결과는 다음 세 조건을 동시에 만족해야 한다.

1. ZIP 화면의 네이비, 민트, 바이올렛 색감과 카드 밀도, 마스코트 분위기를 유지한다.
2. 모든 핵심 UI를 HTML/CSS/React 컴포넌트로 다시 만들어 선택, 편집, 저장, 뒤로가기, 상태 반영이 실제로 동작하게 한다.
3. ZIP의 저해상도 원본을 무리하게 확대하지 않고, 필요한 경우에만 비파괴 파생 에셋을 만들어 선명도를 보완한다.

전체 화면 캡처를 클릭 영역 위에 올리는 방식은 사용하지 않는다. 화면 캡처는 시각 기준으로만 사용하고, 텍스트, 버튼, 카드, 차트, 탭, 입력 필드는 코드로 구현한다.

## 2. 프로젝트 제약

- 원본 `C:\Users\최현규\Desktop\Homepage` 저장소는 수정하지 않는다.
- 모든 작업은 `MajorEvolutionHomepage` 저장소에서만 진행한다.
- 현재 저장소는 Next.js 15, React 19, TypeScript 기반이므로 Expo로 재구축하지 않고 모바일 우선 웹 앱으로 구현한다.
- 실제 AI API를 전공 분석, 아이디어 생성, 논문 이해, 맥락형 도움에 사용한다. 교수 데이터 크롤링, 로그인, OAuth 기반 이메일 자동 발송과 캘린더 계정 직접 연동은 이번 범위에 포함하지 않는다.
- AI 결과와 fallback fixture를 함께 지원하며 사용자의 선택, 편집, 저장, 완료 상태는 브라우저에 지속 저장한다.

## 3. 기준 자료와 우선순위

요구사항이 충돌할 때 다음 순서로 판단한다.

1. 사용자의 최신 지시
2. `design(1).md`의 기능, 카피, UX, 접근성 규칙
3. ZIP `01_SOURCE_SCREENS`의 시각적 분위기와 구성
4. ZIP `02_EXACT_CROPS`, `03_TRANSPARENT_PNG`의 원본 픽셀
5. 기존 Homepage 코드의 구현 방식

### 3.1 확인한 자료

- 원본 보드: 10개, 각 1536 x 1024px
- 직접 크롭: 53개
- 투명 PNG: 16개
- 분리 위치 표시 화면: 10개
- 화면별 에셋 연락판: 10개
- 에셋 카탈로그 PDF: 46페이지
- 전체 기능 명세: S01-S13, 보조 Bottom Sheet, 상태, 접근성, QA, fixture

### 3.2 명세와 ZIP의 주요 충돌 해결

| 항목 | ZIP 화면 | 최종 구현 결정 |
|---|---|---|
| 최초 진입 | 로그인 버튼 노출 | 로그인 제거, `전공 진화 시작하기`와 `샘플 결과 먼저 보기` 제공 |
| 전체 흐름 | 9단계 중심 | `design(1).md`의 S01-S13 흐름 사용 |
| 점수 | 일부 퍼센트와 단일 추천 점수 | 교수 점수는 `%` 없이 `추천 적합도`, 별도 `근거 신뢰도` 표시 |
| 레이더 차트 | 결과의 중심 요소 | 보조 시각화로 유지하고 숫자, 막대, 쉬운 설명을 함께 제공 |
| 하단 탭 | 홈/탐색/추가/프로젝트/마이 | 홈/탐색/실행/보관함/마이 사용 |
| 화면 톤 | 일부 전체 다크 화면 | 전체 앱은 밝은 톤, 네이비는 결과 히어로 카드에 제한적으로 사용 |
| 출처 | 시각적으로 축약 | 추천 이유, 출처 유형, 검증일, 확인 필요 항목을 분리해 표시 |

## 4. 제품 정의

전공진화소는 대학생이 자신의 전공, 관심사, 강점과 제약을 입력하면 전공 확장 방향을 이해하고, 실행 가능한 아이디어를 고른 뒤, 그 아이디어와 연결되는 교수 유형과 첫 행동까지 준비하는 모바일 우선 웹 앱이다.

핵심 경험 문장은 다음과 같다.

> 탐색은 재미있게, 판단은 정확하게, 행동은 부담 없게.

## 5. 목표와 성공 기준

### 5.1 제품 목표

- 사용자가 10초 안에 `전공 -> 아이디어 -> 교수/실행`의 가치를 설명할 수 있다.
- 사용자가 3분 안에 전공 DNA 입력을 완료할 수 있다.
- 사용자가 아이디어를 점수만이 아니라 데이터, 기간, 개인 적합도 기준으로 비교할 수 있다.
- 사용자가 교수 추천의 근거, 출처, 검증일과 직접 확인할 항목을 구분할 수 있다.
- 사용자가 마지막에 30분 이하의 첫 행동을 시작하고 완료할 수 있다.

### 5.2 시각 목표

- 390px 기준 화면에서 ZIP의 네이비 헤드라인, 민트/블루/바이올렛 포인트, 밝은 캠퍼스 분위기를 재현한다.
- 360px, 390px, 430px에서 텍스트, 카드, CTA가 잘리거나 겹치지 않는다.
- 캐릭터와 장식은 핵심 정보보다 먼저 읽히지 않는다.
- 원본 이미지에 포함된 UI 텍스트를 최종 인터페이스 텍스트로 사용하지 않는다.

### 5.3 기능 목표

- S01-S13 주 경로가 끊김 없이 동작한다.
- 새로고침과 브라우저 뒤로가기 후에도 사용자의 선택이 유지된다.
- 저장, 편집, 필터, 비교, 난이도 변경, 4주 범위 적용, 퀘스트 완료가 실제 상태에 반영된다.
- `데모 초기화`로 모든 로컬 상태를 초기 fixture로 되돌릴 수 있다.

## 6. 대상 사용자와 기준 시나리오

### 6.1 대상 사용자

- 프로젝트, 논문, 공모전, 포트폴리오 주제가 필요한 3-4학년 대학생
- 교수 면담을 요청하고 싶지만 준비 방법을 모르는 학생
- 전공과 AI를 연결하고 싶지만 구체적인 데이터와 방법론을 정하지 못한 학생

### 6.2 기준 시나리오

- 주전공: 수학
- 부전공: 식품자원경제
- 관심사: 데이터 분석, AI, ESG, 식품 소비
- 강점: 수학적 모델링, Python, 머신러닝, 설문분석
- 목표: 데이터 분석가 또는 AI 서비스 기획자
- 가용 시간: 4주
- 결과물: 포트폴리오 프로젝트와 교수 면담 준비

## 7. 범위

### 7.1 포함

- 목적 선택
- 6단계 전공 DNA 입력과 검증
- 분석 진행 화면
- 전공 DNA 결과와 전공 진화 리포트
- 연구형, 프로젝트형, 서비스형 아이디어 3개
- 아이디어 2개 선택, 비교, 중요 기준 선택
- 난이도 조절과 4주 범위 자동 조정
- 실현성 게이트
- 편집 가능한 아이디어 패스포트
- 교수 후보 3명, 필터, 저장, 상세
- 추천 이유, 근거 신뢰도, 출처, 검증일, 확인 필요 항목
- 편집 가능한 면담 질문과 이메일 초안
- 논문 초록·본문 AI 구조화 분석
- 첫 30분 행동과 6단계 실행 준비, 4주 실행 계획
- 이메일 앱, 캘린더, Markdown, 인쇄 기반 PDF 내보내기
- 홈, 탐색, 실행, 보관함, 마이의 최소 기능
- loading, empty, error, low-confidence, offline 상태

### 7.2 제외

- 실제 교수 또는 대학 데이터 수집
- 교수 평판, 성격, 친절도, 인기 순위
- 회원가입, 소셜 로그인, 결제
- OAuth 기반 이메일 자동 발송과 캘린더 계정 직접 등록
- 커뮤니티, 후기, 관리자 CMS
- 다크 모드
- 서버 보관형 PDF 파일 생성
- 범용 AI 채팅
- 생성 결과에 대한 외부 출처 자동 검증

## 8. 정보 구조와 라우트

| 화면 | 라우트 | 핵심 상태 변화 |
|---|---|---|
| S01 스플래시 | `/` | 새 여정 또는 샘플 결과 선택 |
| S02 목적 선택 | `/goal` | `goal` 저장 |
| S03 전공 DNA 입력 | `/dna` | `profile`, `dnaStep` 저장 |
| S04 분석 진행 | `/analyzing` | 순차 메시지 후 결과 이동 |
| S05 전공 진화 리포트 | `/evolution-report` | 샘플 모드, 선택 방향 저장 |
| S06 아이디어 랩 | `/ideas` | 저장 아이디어, 비교 대상 2개 선택 |
| S07 아이디어 비교 | `/ideas/compare` | 중요 기준, 최종 아이디어, 난이도 저장 |
| S08 실현성 게이트 | `/feasibility` | 원본/4주 버전 저장 |
| S09 아이디어 패스포트 | `/passport` | 섹션별 편집 내용 저장 |
| S10 교수 추천 | `/professors` | 필터, 저장 교수, 선택 교수 저장 |
| S11 교수 상세 | `/professors/[id]` | 교수 저장, 퀘스트 생성 |
| S12 실행 퀘스트 | `/quest` | 질문/초안 편집, 퀘스트 완료 저장 |
| S13 홈 | `/home` | 현재 프로젝트와 다음 행동 표시 |
| 탐색 | `/explore` | 리포트, 아이디어, 교수 탐색 화면으로 재진입 |
| 보관함 | `/saved` | 저장 아이디어와 교수 목록 |
| 마이 | `/profile` | DNA 요약, 설정, 데모 초기화 |

잘못된 순서로 URL에 직접 진입해도 빈 화면을 보이지 않는다. 필요한 이전 상태가 없으면 샘플 fixture를 주입하고 `샘플 데이터`임을 표시하거나 적절한 이전 단계로 안내한다.

## 9. 화면별 요구사항과 에셋 매핑

| 화면 | 필수 기능 | 시각/에셋 전략 | 완료 조건 |
|---|---|---|---|
| S01 스플래시 | 시작, 샘플 결과 보기 | `02_splash_hero_composite`를 기준으로 코드 카피와 CTA를 분리 | 첫 화면에 가치, 마스코트, CTA가 함께 보임 |
| S02 목적 선택 | 4개 목적 중 1개 선택, 4번째 안내 Sheet | 목적 카드는 코드 구현, `common_header_robot`는 원본 크기 이내 사용 | 선택 전 CTA 비활성, 선택 후 이동 |
| S03 DNA 입력 | 6단계, 필수값 검증, 뒤로가기 | 입력, 칩, 진행 바 모두 코드 구현 | 단계 이동과 상태 복원이 동작 |
| S04 분석 진행 | 3개 메시지 순차 표시, OpenAI 호출 후 자동 이동 | 작은 로봇 포즈와 정적인 회로 장식 사용 | 최소 1.8초 로딩, 지연 안내, 가짜 퍼센트 없음 |
| S05 진화 리포트 | AI DNA 요약, 탐색 방향 5개, 준비 기술, fallback 상태 | 레이더 차트는 SVG/코드로 재구성, 원본 crop은 대조 기준 | 생성 결과와 샘플 결과를 명확히 구분 |
| S06 아이디어 랩 | AI 아이디어 3개, 저장, 2개 선택, 실제 재생성 | 카드와 점수 막대는 코드 구현 | 정확히 2개 선택 시 비교 CTA 활성화 |
| S07 아이디어 비교 | 기준 최대 2개, 세로 비교, 난이도 선택 | `comparison_bars_original`은 참고만 하고 선명한 코드 막대 사용 | 선택 결과 문구와 범위가 즉시 변경 |
| S08 실현성 게이트 | 6개 점검, 4주 적용, 원본 유지 | 방패는 native 크기 또는 Lucide 아이콘, 상태행은 코드 | 적용 후 상태와 CTA가 변경 |
| S09 패스포트 | 7개 섹션, Bottom Sheet 편집 | 문서형 1컬럼 카드, 원본 패널은 구조 참고 | 편집값 저장과 새로고침 복원 |
| S10 교수 추천 | 필터, 저장, 상세, 비교 대상 | 73px 원본 얼굴은 56-64px 이하로 사용 | 이유가 점수보다 먼저 읽힘 |
| S11 교수 상세 | 이유 3개, 출처, 검증일, 확인점, 저장 | 점수 링은 CSS/SVG, 얼굴은 원본 크기 이내 사용 | 가상 데이터와 점수 의미가 명시됨 |
| S12 실행 퀘스트 | 첫 행동, 편집, 복사, 완료, 4주 계획 | `interview_robot`, `confetti`는 작은 축하 영역에 사용 | 완료 시 나머지 섹션과 홈 상태 반영 |
| S13 홈 | 현재 프로젝트, 다음 행동, 저장 수, 최근 기록 | 하단 탭은 Lucide 한 계열로 코드 구현 | 재진입 시 마지막 상태가 정확히 표시 |

### 9.1 보조 화면과 Bottom Sheet

| 보조 UI | 진입 위치 | 동작 |
|---|---|---|
| 추천 적합도 설명 | S10, S11 점수 정보 버튼 | 점수가 성공 확률이나 교수 평가가 아님을 설명 |
| 근거 신뢰도 설명 | S10, S11 신뢰도 배지 | 높음, 보통, 확인 필요의 판정 기준 설명 |
| 맥락형 AI 제안 | S05, S11 | 현재 프로필·방향·아이디어를 서버 AI에 전달해 설명과 면담 문장을 생성 |
| 편집 Sheet | S09, S12 | 키보드 회피, 입력 검증, 변경 저장, 취소 지원 |
| 출처 상세 | S10, S11 | 출처 유형, 제목, 검증일, 추천에 사용한 항목 표시 |
| 논문 이해 | S02 네 번째 목적 | `/paper`에서 실제 AI 구조화 분석 실행 |

모든 Sheet는 바깥 영역, 닫기 버튼, Escape로 닫을 수 있어야 한다. 저장하지 않고 닫을 때는 기존 값을 유지한다.

## 10. 핵심 인터랙션 명세

### 10.1 목적 선택

- 카드 전체가 터치 영역이다.
- 한 번에 하나만 선택한다.
- 논문 이해 목적은 `/paper`로 이동해 초록 또는 본문 입력을 받는다.

### 10.2 DNA 입력

- 주전공, 관심 주제 1개 이상, 목적 1개가 필수다.
- 관심 주제는 최대 5개, 진로 목표는 최대 2개다.
- 경험 입력은 160자로 제한한다.
- 필드 오류는 해당 필드 아래에 표시하고 입력값을 삭제하지 않는다.

### 10.3 아이디어 선택과 비교

- 저장과 비교 선택은 별도 상태다.
- 비교 대상은 정확히 2개까지만 허용한다.
- 중요 기준은 최대 2개이며 현재 생성 결과의 점수는 바꾸지 않고 강조 순서만 변경한다.
- 난이도 변경 시 기간, 데이터 범위, 방법, 결과물 설명이 함께 변경된다.

### 10.4 실현성 조정

- `4주 버전 적용하기`는 `feasibilityVersion`을 변경한다.
- 적용 후 상단 상태가 `시작 가능`으로 바뀐다.
- 원래 범위로 되돌릴 수 있다.

### 10.5 편집과 저장

- 패스포트, 면담 질문, 이메일 초안은 Bottom Sheet 또는 인라인 편집으로 수정한다.
- 저장 시 즉시 로컬 상태에 반영하고 짧은 완료 피드백을 준다.
- 저장 아이콘은 낙관적으로 반영하되 중복 항목을 만들지 않는다.

### 10.6 퀘스트

- 첫 행동은 25분 예상과 명확한 완료 조건을 보여준다.
- 첫 행동 완료 시 나머지 실행 자료가 열리고 각 단계를 개별 완료할 수 있다.
- 이메일은 편집, 복사, 기본 이메일 앱 열기를 제공하고 사용자 확인 없이 자동 발송하지 않는다.
- 캘린더는 표준 ICS 파일을 내려받아 사용자가 원하는 캘린더에 등록한다.
- 프로젝트 문서는 Markdown과 인쇄 대화상자를 통한 PDF 저장을 제공한다.
- 완료 상태는 홈의 진행률과 최근 활동에 반영한다.

## 11. 상태와 데이터

Zustand `persist` 미들웨어와 `localStorage`를 사용한다. 저장 키에는 버전을 포함해 fixture 변경 시 안전하게 마이그레이션한다.

```ts
type PrototypeState = {
  version: 1;
  goal: 'explore-major' | 'find-topic' | 'find-professor' | 'understand-paper' | null;
  dnaStep: number;
  profile: StudentProfile;
  isSampleMode: boolean;
  selectedTrendId: string | null;
  selectedIdeaIds: string[];
  selectedIdeaId: string | null;
  comparisonCriteria: ComparisonCriterion[];
  difficulty: 'starter' | 'project' | 'advanced' | 'research';
  feasibilityVersion: 'original' | 'four-week';
  passport: EditablePassport;
  professorFilter: ProfessorFilter;
  selectedProfessorId: string | null;
  completedQuestIds: string[];
  savedIdeaIds: string[];
  savedProfessorIds: string[];
  editedQuestions: string[];
  editedEmailDraft: string;
  resetDemo: () => void;
};
```

### 11.1 데이터 원칙

- 점수와 카피는 모든 화면에서 동일해야 한다.
- 교수 이름과 학교, 연구실은 `프로토타입용 가상 데이터`로 표시한다.
- 실제 외부 URL이 없는 출처는 가짜 링크를 만들지 않고 출처 상세 Sheet를 연다.
- AI 결과는 사실처럼 단정하지 않고 입력과의 연결 이유를 설명한다.
- 오류, 빈 상태, 낮은 신뢰도도 fixture 토글로 재현 가능하게 한다.

## 12. 시각 시스템

### 12.1 핵심 토큰

- 제목: `#061238`, `#071A4A`
- 민트: `#35C4B3`
- 블루: `#4F7DF3`
- 바이올렛: `#7658F5`
- CTA 끝색: `#6139E8`
- 기본 배경: `#F7F9FF`
- 카드: `#FFFFFF`
- 기본 보더: `#E3E8F4`
- 본문: `#53627F`
- 기본 폰트: 저장소의 Pretendard Variable

색상, 간격, radius, 그림자, typography는 CSS 변수와 TypeScript 토큰으로 중앙 관리한다. 화면 컴포넌트에 임의 HEX를 직접 쓰지 않는다.

### 12.2 레이아웃

- 모바일 360-430px: 단일 컬럼
- 기준 좌우 패딩: 20px, 360px에서는 16px
- 앱 콘텐츠 최대 폭: 560px
- 데스크톱: 가짜 스마트폰 프레임 없이 중앙 앱 컬럼을 사용
- Sticky CTA 높이: 82px + safe area
- 마지막 콘텐츠 하단 여백: 최소 112px
- 고정 CTA와 하단 탭은 동시에 표시하지 않는다.

### 12.3 컴포넌트 원칙

- 버튼, 입력, 카드, 칩, Sheet, 탭, 상태 배지는 공통 컴포넌트로 구현한다.
- 아이콘은 `lucide-react` 한 계열만 사용한다.
- 점수 링, 막대, 레이더 차트는 데이터 기반 SVG/CSS로 구현한다.
- 모든 아이콘 버튼에 tooltip 또는 접근성 레이블을 제공한다.
- 한 화면의 Primary CTA는 하나만 둔다.

## 13. 이미지와 에셋 처리 계획

### 13.1 원본 보존

- `public/major-evolution-assets`는 읽기 전용 기준본으로 취급한다.
- 원본 crop을 덮어쓰거나 이름을 변경하지 않는다.
- 보정본은 `public/major-evolution-assets/derived`에 별도 저장한다.
- 파생 파일명에 원본 이름과 배율 또는 용도를 포함한다.

### 13.2 사용 우선순위

1. HTML/CSS/React로 선명하게 재구성 가능한 UI는 코드로 구현
2. 투명 PNG가 있는 마스코트와 장식은 native 크기 이내 사용
3. 원본 보드에서 더 큰 실제 픽셀을 얻을 수 있을 때만 재크롭
4. 목표 크기에서 흐릿함이 확인된 비텍스트 장식은 비파괴 2x 보정본 제작
5. 생성형 보정은 로봇 또는 캠퍼스 배경처럼 비식별 장식에만 제한적으로 사용

### 13.3 금지

- 작은 교수 얼굴을 생성형 도구로 복원하거나 다른 얼굴로 바꾸지 않는다.
- 텍스트가 박힌 전체 화면 이미지를 실제 UI로 사용하지 않는다.
- 낮은 해상도의 왕관, 방패, 하단 탭 아이콘을 크게 확대하지 않는다.
- 보정본 때문에 마스코트 표정, 색, 비율, 소품이 달라지지 않게 한다.
- 원본과 보정본의 시각 차이가 큰 경우 보정본을 채택하지 않는다.

### 13.4 자산별 결정

- 스플래시: `02_splash_hero_composite`를 목표 폭 340-371px에서 사용
- 헤더 로봇: 44-64px 범위에서 투명 PNG 사용
- 레이더 차트: 원본 이미지를 붙이지 않고 코드로 재구성
- 아이디어 카드, 비교 막대, 실현성 행: 전부 코드로 재구성
- 교수 얼굴: 73px 원본을 56-64px로 축소 사용
- 점수 링: 코드로 재구성
- 면담 로봇과 콘페티: native 크기 근처의 축하 배너에서 사용
- 하단 탭: crop 대신 Lucide 아이콘으로 동일한 의미와 간격 재현

## 14. 기술 구현 구조

### 14.1 기술 스택

- Next.js 15 App Router
- React 19
- TypeScript strict mode
- Tailwind CSS 4 + CSS variables
- Zustand + persist
- Framer Motion
- Lucide React
- Next Image
- Playwright 기반 핵심 흐름 및 화면 QA

### 14.2 권장 폴더

```text
app/
  (journey)/
    goal/page.tsx
    dna/page.tsx
    analyzing/page.tsx
    evolution-report/page.tsx
    ideas/page.tsx
    ideas/compare/page.tsx
    feasibility/page.tsx
    passport/page.tsx
    professors/page.tsx
    professors/[id]/page.tsx
    quest/page.tsx
  home/page.tsx
  saved/page.tsx
  profile/page.tsx
  page.tsx
components/
  app-shell/
  primitives/
  feedback/
  dna/
  ideas/
  professors/
  quest/
data/
  prototype.ts
design/
  tokens.ts
store/
  prototype-store.ts
tests/
  e2e/
```

### 14.3 기존 코드 전환

현재 저장소에는 기존 `모두의 창업 솔루션` 사이트의 메타데이터, 페이지, API, Supabase 코드가 남아 있다. 구현 단계에서 다음을 수행한다.

- 루트 metadata, manifest, sitemap, robots, Open Graph를 전공진화소로 교체
- 기존 Navbar, Hero, Footer, SmoothScroll, Preloader, 커스텀 커서 의존 제거
- 기존 사업 소개 페이지와 전공진화소 라우트 충돌 제거
- 기존 contact, chat, admin API와 Supabase 코드 제거 또는 앱에서 완전히 분리
- 기존 마케팅용 전역 CSS를 전공진화소 토큰 기반으로 정리
- 사용하지 않는 사업 이미지와 컴포넌트는 최종 빌드 전에 제거

이 작업은 새 저장소에서만 하며 원본 Homepage 저장소에는 반영하지 않는다.

## 15. 접근성

- 일반 텍스트 명암비 4.5:1 이상
- 큰 텍스트 명암비 3:1 이상
- 모든 터치 타깃 44 x 44px 이상
- 키보드만으로 전체 주 경로 완료 가능
- `:focus-visible` 포커스 링 제공
- 장식 이미지는 빈 대체 텍스트 또는 접근성 트리 제외
- 정보 이미지는 동등한 텍스트 설명 제공
- 상태는 색만으로 전달하지 않고 아이콘과 문구 병행
- 점수는 `추천 적합도 89점, 근거 신뢰도 높음`으로 읽힘
- 글자 크기 130%에서도 주요 CTA와 입력이 잘리지 않음
- `prefers-reduced-motion`에서 반복 애니메이션, count-up, pulse 제거
- Bottom Sheet는 열릴 때 포커스를 가두고 닫힐 때 호출 버튼으로 복원

## 16. 모션

- 화면 전환: 220ms opacity + translateY 8px
- 카드 선택: 120ms press feedback
- 결과 카드: 60ms 간격 stagger, 최초 1회
- 점수/막대: 360-700ms, 최초 1회
- 분석 pulse: 1400ms의 약한 반복
- 스크롤을 지연하거나 텍스트를 가리는 애니메이션은 사용하지 않는다.

## 17. 오류와 특수 상태

| 상태 | 처리 |
|---|---|
| Loading | 500ms 이상 예상 시 skeleton 또는 단계형 진행 UI |
| Empty | 작은 아이콘/마스코트, 상태 설명, CTA 1개 |
| Error | 사용자 문구, 다시 시도, 이전 화면 제공 |
| Low confidence | 부족한 출처와 직접 확인할 항목 표시 |
| Offline | 저장 결과는 열람 가능, 새 분석 동작만 제한 |
| 직접 URL 진입 | fixture 주입 또는 선행 화면 안내 |
| 저장소 버전 불일치 | 로컬 상태 마이그레이션 또는 안전 초기화 안내 |

## 18. 개인정보, 보안, 성능

### 18.1 개인정보와 보안

- 학교, 전공, 관심사, 경험 입력은 브라우저 `localStorage`에만 저장한다.
- 이번 프로토타입은 사용자 입력을 서버, 분석 도구, 외부 AI로 전송하지 않는다.
- 별도 분석 스크립트나 광고 추적기를 추가하지 않는다.
- 마이 화면에서 `데모 초기화`를 실행하면 관련 로컬 저장 키를 모두 삭제한다.
- 사용자 입력을 `dangerouslySetInnerHTML`로 렌더링하지 않는다.
- 새 저장소에 남아 있는 기존 contact, chat, admin API는 최종 앱에서 노출하지 않는다.

### 18.2 성능과 호환성

- Splash LCP: 모바일 Fast 4G 기준 2.5초 이하 목표
- CLS: 0.1 이하
- INP: 200ms 이하
- 이미지에 고정 width/height 또는 aspect-ratio를 지정해 레이아웃 이동을 막는다.
- 10개 원본 보드와 연락판은 런타임에 한꺼번에 내려받지 않는다.
- 화면별 장식 이미지는 필요한 라우트에서만 지연 로딩한다.
- 기존 Three.js, Lenis, 3D 쇼케이스 코드는 최종 앱 번들에서 제외한다.
- 지원 브라우저: 최신 Chrome, Edge, Safari와 iOS Safari의 현재 주요 버전

## 19. QA 계획

### 19.1 기능 QA

- 목적 선택부터 홈까지 전체 주 경로 자동화
- DNA 필수값, 최대 선택 수, 글자 수 검증
- 뒤로가기와 새로고침 후 입력 유지
- 아이디어 2개 선택 제한과 비교 진입
- 난이도 변경에 따른 범위 문구 변경
- 4주 버전 적용과 원본 복원
- 패스포트 편집 저장
- 교수 필터, 저장, 상세 이동
- 퀘스트 편집, 복사, 완료
- 홈 진행률 반영
- 데모 초기화

### 19.2 시각 QA

다음 viewport에서 각 핵심 화면의 스크린샷을 저장하고 ZIP 원본 및 PRD와 대조한다.

- 360 x 800
- 390 x 844
- 430 x 932
- 768 x 1024
- 1440 x 900

검수 항목:

- 텍스트 잘림과 겹침 0건
- CTA와 하단 탭이 콘텐츠를 가리는 문제 0건
- H1 최대 2줄
- 한 화면의 강한 그라데이션 최대 2곳
- 카드 radius, 보더, 그림자 일관성
- 원본 에셋의 비율 왜곡 0건
- 흐릿하거나 계단 현상이 눈에 띄는 에셋 0건
- 빈 이미지, 깨진 이미지, 레이아웃 이동 0건
- 390px 기준 오버레이 비교에서 주요 블록의 위치와 크기 차이 4px 이내
- 동일 요소의 색상은 지정 토큰과 일치하고 임의 근사색을 사용하지 않음
- 폰트 렌더링 차이는 허용하되 기준선, 행간, 줄 수는 참조 화면과 일치

### 19.3 접근성 QA

- 키보드 탭 순서
- 포커스 가시성
- 버튼과 입력 accessible name
- 색상 대비
- 130% 텍스트 확대
- Reduce Motion
- Sheet의 focus trap과 Escape 닫기

### 19.4 빌드 QA

- TypeScript 오류 0건
- production build 성공
- hydration 오류 0건
- 브라우저 console error 0건
- 사용하지 않는 기존 사업 API가 노출되지 않음

## 20. 구현 단계와 통과 조건

### Phase 0. 기준 고정

- 에셋 인벤토리와 화면 매핑 확정
- PRD와 디자인 우선순위 확정
- 기준 viewport 확정

통과 조건: S01-S13의 기능과 에셋 출처가 모두 문서화됨.

### Phase 1. 앱 기반

- 전공진화소 metadata와 전역 토큰
- AppShell, TopAppBar, StickyActionBar, BottomTabBar
- 버튼, 카드, 칩, 입력, Sheet
- fixture와 persisted store

통과 조건: 공통 컴포넌트의 360/390/430px 샘플 화면이 깨지지 않음.

### Phase 2. 입력 여정

- S01-S04
- 목적 선택, 6단계 입력, 검증, 분석 진행

통과 조건: 새 사용자 흐름과 샘플 결과 흐름 모두 S05에 도달.

### Phase 3. 아이디어 여정

- S05-S09
- 리포트, 아이디어, 비교, 난이도, 실현성, 패스포트

통과 조건: 사용자가 아이디어를 선택하고 편집 가능한 패스포트를 완성.

### Phase 4. 교수와 실행 여정

- S10-S13과 saved/profile 최소 화면
- 교수 근거, 출처, 실행 퀘스트, 홈 반영

통과 조건: 교수 저장과 퀘스트 완료가 홈과 보관함에 반영.

### Phase 5. 이미지 정밀 보정

- 브라우저 실측 스크린샷에서 흐릿한 에셋 식별
- 필요한 자산만 derived 버전 생성
- 원본과 보정본 side-by-side 비교

통과 조건: 보정으로 인한 형태 변화 없이 목표 크기에서 시각 결함이 없음.

### Phase 6. 최종 QA

- 전체 기능 자동화
- 5개 viewport 시각 검수
- 접근성, 빌드, console 검수
- 레거시 코드와 불필요 자산 정리

통과 조건: Definition of Done 전 항목 통과.

## 21. 주요 위험과 대응

| 위험 | 영향 | 대응 |
|---|---|---|
| ZIP 화면과 최신 명세의 흐름 차이 | 화면은 비슷하지만 기능이 어긋남 | 기능은 명세, 시각은 ZIP이라는 우선순위 유지 |
| 작은 원본 에셋 | 확대 시 흐릿함 | native 크기 사용, UI는 코드 재구성, 제한적 derived 보정 |
| 전체 화면 이미지를 UI로 오용 | 기능과 접근성 상실 | 캡처는 기준으로만 사용, 인터랙션은 컴포넌트 구현 |
| 기존 Homepage 코드 잔존 | 메타데이터/API/스타일 충돌 | 새 앱 전환 단계에서 레거시 의존 제거 |
| 긴 모바일 화면 | CTA가 콘텐츠를 가림 | 112px 하단 여백, Sticky CTA와 탭 상호 배타 |
| fixture가 실제 교수 정보처럼 보임 | 신뢰와 법적 위험 | 모든 교수 화면에 가상 데이터 표시, 가짜 외부 링크 금지 |
| 로컬 상태 스키마 변경 | 저장 데이터 오류 | 버전 키와 마이그레이션/초기화 경로 제공 |

## 22. Definition of Done

### 기능

- [ ] S01-S13 주 경로가 끊김 없이 동작한다.
- [ ] 입력, 선택, 저장, 편집, 완료 상태가 새로고침 후 유지된다.
- [ ] 모든 노출 버튼이 동작하거나 명확한 준비 예정 안내를 제공한다.
- [ ] 잘못된 직접 URL 진입에도 복구 경로가 있다.
- [ ] 데모 초기화가 동작한다.

### 디자인

- [ ] ZIP의 핵심 색감, 카드 밀도, 마스코트 분위기를 유지한다.
- [ ] 360, 390, 430px 화면에서 잘림과 겹침이 없다.
- [ ] 전체 화면 캡처를 실제 UI로 사용하지 않는다.
- [ ] 에셋 비율 왜곡과 눈에 띄는 확대 열화가 없다.
- [ ] 모든 핵심 화면의 비교 스크린샷이 남아 있다.

### 신뢰

- [ ] 교수 추천에 적합도, 신뢰도, 이유, 출처, 검증일이 있다.
- [ ] 교수와 학교 정보가 가상 데이터임을 표시한다.
- [ ] 교수 성격, 평판, 성공 가능성을 생성하지 않는다.
- [ ] AI 결과가 사용자의 입력과 어떤 식으로 연결됐는지 설명한다.

### 접근성

- [ ] 모든 터치 타깃이 44 x 44px 이상이다.
- [ ] 일반 텍스트 명암비가 4.5:1 이상이다.
- [ ] 키보드만으로 주 경로를 완료할 수 있다.
- [ ] 130% 글자 크기와 Reduce Motion을 지원한다.

### 품질

- [ ] production build가 성공한다.
- [ ] TypeScript, hydration, console 오류가 없다.
- [ ] 브라우저 자동화와 시각 QA를 통과한다.
- [ ] 기존 Homepage 저장소에는 변경이 없다.

## 23. 구현 착수 결정

별도의 디자인 재기획 없이 구현을 시작할 수 있다. 첫 구현은 Phase 1의 전공진화소 전역 토큰, persisted store, AppShell과 공통 컴포넌트부터 시작한다. 이후 S01부터 S13까지 순차적으로 연결하고, 각 단계마다 모바일 스크린샷과 기능 테스트를 함께 검수한다.
